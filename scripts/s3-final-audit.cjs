// RC4 replaces the rough :any regex and historical 6568 ceiling with AST inventory.
const {audit}=require('./audit-typescript.cjs');
const report=audit();console.log(JSON.stringify(report));if(!report.pass)process.exitCode=1;
